import { tv } from "tailwind-variants";

export const StyleButtton = tv({
  base: "w-full lg:w-fit py-3 px-5 bg-highlight capitalize text-light rounded-sm text-center uppercase tracking-widest text-sm",
});
